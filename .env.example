# Copy to .env and fill in. NEVER commit the real .env.
SECRET_KEY=change-me-to-a-long-random-string
# Leave DATABASE_URL unset locally to use SQLite; set to a Postgres URL in production.
# DATABASE_URL=postgresql://user:pass@host:5432/routeoptimizer
# Phase 2 — OpenRouteService key (geocoding + distance matrix).
ORS_API_KEY=
FUEL_COST_PER_MILE=0.65
