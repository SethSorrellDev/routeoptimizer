[pytest]
testpaths = tests

# Put the project root on sys.path so `pytest` behaves the same as
# `python -m pytest`. Without this, a bare `pytest` (which is what CI and most
# IDEs run) cannot import the `app` package or the seed modules.
pythonpath = .

filterwarnings =
    ignore::DeprecationWarning:sqlalchemy.*
